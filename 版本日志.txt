1、报警信息820：“从机1忙”改为“从机1不在自动状态”，依次类推
2、主从机状态显示：从机地址从2开始，显示的时候显示为“从机1”，和报警保持一致
3、取物失败后弹出提示框，如果这个时候把主机旋钮打到停止状态，提示框无法关闭，
只能点击右上角的关闭按钮才能关闭。可以判断状态变为停止时，自动隐藏对话框。
4、报警信344：“CAN类型重选”改为“单机联机模式更改”
5、在停止界面增加按钮“单机/联机”，变量为系统参数中mem_sys.useset.eject（顶针使用/不使用）
0为单机，1为联机，默认为单机模式，该按钮只有在停止页面的停止状态下才可以点击，其他情况下灰显
